<html lang="en">
    <%- include('../partials/head.ejs') %>
    <body>
          <%- include('../partials/nav.ejs') %>
          
        <div class="blogs content">
            <h1><%= global.churchName %></h1>
            <a href="/incomes/create/<%= churchId %>">Add Income</a>
            <h2>All Incomes</h2>
            <% if(incomes == null ) { %>
                <p>No incomes data available</p>
               <% } else { %>
                <% if(incomes.length > 0) { %>
                    <% incomes.forEach(income => { %>
                        <a class="single" href="/incomes/<%= income.incomeId %>" >
                    <h3 class="title"><%= income.fundType %> </h3>
                    <h3 class="title"><%= income.incomeDate.toISOString().substr(0,10) %> </h3>
                    <h3 class="title"><%= income.cashAmount %> </h3>
                    <h3 class="title"><%= income.checkAmount %> </h3>
                    <h3 class="title"><%= income.coinAmount %> </h3>
                    <h3 class="title"><%= income.total %> </h3>
                    <p class="snippet"><%= income.checkNumber %></p>
                    </a>

                    <% }) %>
                <% } %>
           <% } %>
            
        </div>
        <%- include('../partials/footer.ejs') %>
    </body>

</html>